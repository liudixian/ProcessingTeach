//在网格中绘图
color c ;

void setup(){
  size(600, 600, P2D);
  //ellipse(300,300,280,280);
  frameRate(60); //绘制的速度
  pixelDensity(2);
  background(255);
}


void draw(){
  colorMode(HSB, 100);
  for(int i =0; i <width; i ++) {
    
  }
}
